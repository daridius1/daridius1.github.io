---
interface Props {
    title: string;
    description: string;
    date: string;
    slug: string;
}

const { title, description, date, slug } = Astro.props;
---

<div
    class="card bg-white/5 border border-white/10 shadow-xl backdrop-blur-md hover:bg-white/10 transition-all group overflow-hidden"
>
    <div class="card-body p-6">
        <div class="flex justify-between items-center mb-3">
            <span
                class="text-[10px] font-bold uppercase tracking-widest text-white/40"
                >{date}</span
            >
        </div>
        <h3
            class="card-title text-xl font-black tracking-tighter text-white group-hover:text-primary transition-colors"
        >
            {title}
        </h3>
        <p class="text-sm text-white/60 line-clamp-2 my-2 leading-relaxed">
            {description}
        </p>
        <div class="card-actions justify-end mt-4">
            <a
                href={`/blog/${slug}`}
                class="btn btn-sm btn-ghost gap-2 text-xs font-bold uppercase tracking-tighter"
            >
                Leer más
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="h-4 w-4 transition-transform group-hover:translate-x-1"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
                </svg>
            </a>
        </div>
    </div>
</div>
